{
  "name": "att-phone-logs",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "dev:api": "npm run -w apps/api start:dev",
    "dev:web": "npm run -w apps/web dev",
    "import:phone-logs": "npm run -w apps/api import:phone-logs",
    "start:api": "npm run -w apps/api start"
  }
}
